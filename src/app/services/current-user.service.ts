export class CurrentUserService {
// experiment settings
  static ideateSeconds = 5*60 / 10;
  static chatSeconds = 8*60 / 16;
  static numSinglesNeeded = 2;
  static teamSize = 2;

// things to store across components
  static userKey: string;
  static roomKey: string;
  static partnerKey: any;
  static number: string;

}

